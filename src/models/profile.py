# src/models/profile.py
# Profile entity persistence is integrated directly into the UserAccount model's profile_info attribute.
# For simplicity and given the low user count assumption, a separate Profile entity and persistence layer is not created.
